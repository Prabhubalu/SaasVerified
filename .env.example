# Database
DATABASE_URL="postgresql://user:password@localhost:5432/saasverified?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# OAuth Providers
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Site Configuration
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# SEO Verification
GOOGLE_VERIFICATION="your-google-verification-code"

# Google Analytics 4 (optional — lead form conversion uses gtag generate_lead)
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"

# Vtiger — VTAP incoming webhook for Leads (create/update by email on CRM side)
# Copy endpoint + token from API Designer → createleads → Documentation / Security.
VTIGER_WEBHOOK_URL="https://saasverify.od2.vtiger.com/restapi/vtap/webhook/createleads"
VTIGER_WEBHOOK_TOKEN="CvwevTFOHUlCWwLU1wad8wVM"
# If Security uses URL parameter (common), set the param name from Vtiger (often "token"):
# VTIGER_WEBHOOK_TOKEN_QUERY_PARAM="token"
# If Security uses header instead, leave QUERY_PARAM unset (API Designer default header name: Token):
# VTIGER_WEBHOOK_TOKEN_HEADER="Token"
# DEBUG_VTIGER="true"
